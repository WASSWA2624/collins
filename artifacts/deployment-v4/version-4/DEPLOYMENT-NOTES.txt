Collins Version 4 deployment package

Folders:
- backend-v4/: cPanel/DirectAdmin Node.js application root.
- frontend-v4/: static Expo web export for the public web root.
- android-apk-v4/: signed Android release APK and Gradle output metadata.

Backend deployment:
1. Upload the contents of backend-v4/ to the Node.js app root, for example /home/zelahco/collins-backend.
2. Use Node.js 20 or newer.
3. Use cpanel-start.cjs as the startup file.
4. Review .env.production on the server before restart.
5. Run npm install from the backend app root.
6. Run npm run db:migrate:deploy from the backend app root.
7. Restart the Node.js app and verify /ready and /api/v1/health.

Frontend deployment:
1. Upload the contents of frontend-v4/ to the web document root.
2. Keep .htaccess with the upload so client-side routes resolve to index.html.
3. The static web export uses Unix-style relative asset paths generated by Expo and is compatible with Linux/cPanel document roots.
4. The frontend build is configured for https://api.zelah.co.ug/api/v1 through the production env file.
5. Frontend production build number is 4.

Android APK:
1. Install android-apk-v4/collins-api-zelah-release-v4.apk on Android devices.
2. The APK is a signed release build for com.collins.android.
3. Android versionCode is 4 and versionName is 1.0.0.
4. The embedded bundle was built with the production env file.

POSIX/cPanel compatibility:
- Deployment instructions use Unix/Linux paths such as /home/zelahco/collins-backend.
- The web package includes .htaccess rewrite rules for Apache/LiteSpeed cPanel hosting.
- Runtime scripts use Node.js entry points and avoid Windows-only shell wrappers in production startup.
- Artifact folder and zip names use lowercase POSIX-style hyphenated names.

Validation summary:
- Local database migrations are up to date through 20260511110000_patient_age_days.
- Backend Prisma generation was current.
- Backend lint passed with no warnings.
- Backend tests passed: 144/144.
- Frontend Jest passed: 249 suites, 3345 tests passed, 12 skipped.
- Frontend production web export completed.
- Android release APK build completed with versionCode 4.

Notes:
- Version 1, Version 2, and Version 3 artifacts in the artifacts folder were not modified.
- The deployable package intentionally excludes source tests, node_modules, cache folders, debug logs, and development planning files.
- The static web export contains assets/node_modules paths generated by Expo; these are static asset paths, not installable dependencies.
