iOS build status for Collins Version 5

No .ipa/.ipk artifact is included because this Windows workspace cannot run a local iOS build and EAS cloud builds failed before build start due to missing non-interactive Apple signing credentials.

Attempted commands:
- npx eas-cli build --platform ios --profile preview --non-interactive --wait --json
- npx eas-cli build --platform ios --profile production --non-interactive --wait --json

Resolution:
Run EAS iOS build interactively after configuring Apple distribution credentials, then add the downloaded .ipa to this folder.
The local app config includes ios.bundleIdentifier=com.collins.ios and ios.buildNumber=5.
