/**
 * Tenant Detail Route
 * (main)/settings/tenants/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { TenantDetailScreen } from '@platform/screens';

export default function TenantDetailRoute() {
  return <TenantDetailScreen />;
}
