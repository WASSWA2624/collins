/**
 * Tenants List Route
 * (main)/settings/tenants
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { TenantListScreen } from '@platform/screens';

export default function TenantsListRoute() {
  return <TenantListScreen />;
}
