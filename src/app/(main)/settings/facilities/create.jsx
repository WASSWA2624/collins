/**
 * Facility Create Route
 * (main)/settings/facilities/create
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { FacilityFormScreen } from '@platform/screens';

export default function FacilityCreateRoute() {
  return <FacilityFormScreen />;
}
