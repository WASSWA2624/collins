/**
 * Facilities List Route
 * (main)/settings/facilities
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { FacilityListScreen } from '@platform/screens';

export default function FacilitiesListRoute() {
  return <FacilityListScreen />;
}
