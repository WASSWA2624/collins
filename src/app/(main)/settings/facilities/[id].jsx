/**
 * Facility Detail Route
 * (main)/settings/facilities/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { FacilityDetailScreen } from '@platform/screens';

export default function FacilityDetailRoute() {
  return <FacilityDetailScreen />;
}
