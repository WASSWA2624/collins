/**
 * Branch Detail Route
 * (main)/settings/branches/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { BranchDetailScreen } from '@platform/screens';

export default function BranchDetailRoute() {
  return <BranchDetailScreen />;
}
