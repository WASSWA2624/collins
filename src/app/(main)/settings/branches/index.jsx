/**
 * Branches List Route
 * (main)/settings/branches
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { BranchListScreen } from '@platform/screens';

export default function BranchesListRoute() {
  return <BranchListScreen />;
}
