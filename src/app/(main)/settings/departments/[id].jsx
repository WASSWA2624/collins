/**
 * Department Detail Route
 * (main)/settings/departments/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { DepartmentDetailScreen } from '@platform/screens';

export default function DepartmentDetailRoute() {
  return <DepartmentDetailScreen />;
}
