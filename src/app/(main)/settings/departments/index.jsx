/**
 * Departments List Route
 * (main)/settings/departments
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { DepartmentListScreen } from '@platform/screens';

export default function DepartmentsListRoute() {
  return <DepartmentListScreen />;
}
