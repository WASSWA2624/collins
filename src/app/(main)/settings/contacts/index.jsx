/**
 * Contacts List Route
 * (main)/settings/contacts
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { ContactListScreen } from '@platform/screens';

export default function ContactsListRoute() {
  return <ContactListScreen />;
}
