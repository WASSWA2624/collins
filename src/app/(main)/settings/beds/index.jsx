/**
 * Beds List Route
 * (main)/settings/beds
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { BedListScreen } from '@platform/screens';

export default function BedsListRoute() {
  return <BedListScreen />;
}
