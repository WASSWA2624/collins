/**
 * Bed Detail Route
 * (main)/settings/beds/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { BedDetailScreen } from '@platform/screens';

export default function BedDetailRoute() {
  return <BedDetailScreen />;
}
