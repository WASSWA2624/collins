/**
 * Room Detail Route
 * (main)/settings/rooms/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { RoomDetailScreen } from '@platform/screens';

export default function RoomDetailRoute() {
  return <RoomDetailScreen />;
}
