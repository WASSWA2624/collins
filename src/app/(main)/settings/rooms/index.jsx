/**
 * Rooms List Route
 * (main)/settings/rooms
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { RoomListScreen } from '@platform/screens';

export default function RoomsListRoute() {
  return <RoomListScreen />;
}
