/**
 * User Session Detail Route
 * (main)/settings/user-sessions/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserSessionDetailScreen } from '@platform/screens';

export default function UserSessionDetailRoute() {
  return <UserSessionDetailScreen />;
}
