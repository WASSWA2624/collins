/**
 * User Sessions List Route
 * (main)/settings/user-sessions
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserSessionListScreen } from '@platform/screens';

export default function UserSessionsListRoute() {
  return <UserSessionListScreen />;
}
