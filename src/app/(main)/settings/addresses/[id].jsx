/**
 * Address Detail Route
 * (main)/settings/addresses/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { AddressDetailScreen } from '@platform/screens';

export default function AddressDetailRoute() {
  return <AddressDetailScreen />;
}
