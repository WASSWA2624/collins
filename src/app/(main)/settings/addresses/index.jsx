/**
 * Addresses List Route
 * (main)/settings/addresses
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { AddressListScreen } from '@platform/screens';

export default function AddressesListRoute() {
  return <AddressListScreen />;
}
