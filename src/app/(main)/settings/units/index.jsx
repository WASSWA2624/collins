/**
 * Units List Route
 * (main)/settings/units
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UnitListScreen } from '@platform/screens';

export default function UnitsListRoute() {
  return <UnitListScreen />;
}
