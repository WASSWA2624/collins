/**
 * Unit Detail Route
 * (main)/settings/units/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UnitDetailScreen } from '@platform/screens';

export default function UnitDetailRoute() {
  return <UnitDetailScreen />;
}
