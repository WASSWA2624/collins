/**
 * Ward Detail Route
 * (main)/settings/wards/[id]
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { WardDetailScreen } from '@platform/screens';

export default function WardDetailRoute() {
  return <WardDetailScreen />;
}
