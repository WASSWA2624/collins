/**
 * Wards List Route
 * (main)/settings/wards
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { WardListScreen } from '@platform/screens';

export default function WardsListRoute() {
  return <WardListScreen />;
}
