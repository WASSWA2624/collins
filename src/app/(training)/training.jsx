/**
 * Training Home Route
 * (training)/training → /training — Training home (sidebar link target).
 */
import React from 'react';
import { TrainingHomeScreen } from '@platform/screens';

export default function TrainingRoute() {
  return <TrainingHomeScreen />;
}
