# ============================================
# Collins - Environment Variables Template
# ============================================
# Copy this file to `.env`, then update with your actual values.
# Never commit `.env` to version control.
#
# IMPORTANT SECURITY NOTE (Expo):
# Any `EXPO_PUBLIC_*` variable is bundled into the client and is PUBLIC.
# Do NOT put secrets (API keys, tokens, credentials) in client env vars.
# Use env vars only for non-sensitive configuration (e.g., base URLs, public contact info).
# ============================================

# ============================================
# Runtime Environment
# Source of truth: src/config/env.js
# ============================================
# Node environment: development, test, production
NODE_ENV="development"

# App environment label used in the UI/config (optional; defaults to NODE_ENV)
# Values: development, staging, production
# EXPO_PUBLIC_APP_ENVIRONMENT="development"

# ============================================
# Backend API (REQUIRED for online features)
# Source of truth: src/config/env.js + src/config/endpoints.js
# ============================================
# Base URL for the backend API.
# Examples:
# - Dev (local):  http://localhost:3000
# - Prod:         https://api.example.com
# Must include protocol (http:// or https://). Trailing slash is allowed (it is normalized).
EXPO_PUBLIC_API_BASE_URL="http://localhost:3000"

# API version namespace used for endpoint construction (optional; defaults to "v1")
# Result: {API_BASE_URL}/api/{API_VERSION}/...
EXPO_PUBLIC_API_VERSION="v1"

# ============================================
# Build Metadata (optional overrides)
# Source of truth: src/config/env.js
# ============================================
# App version override (defaults to "0.1.0" if not set)
EXPO_PUBLIC_APP_VERSION="0.1.0"

# Build number (defaults to "0" if not set)
EXPO_PUBLIC_BUILD_NUMBER="0"

# ============================================
# Support / Contact (optional)
# Source of truth: src/config/env.js
# Used in: src/platform/components/navigation/GlobalFooter/*
# ============================================
# Public support email shown in the footer (optional)
# EXPO_PUBLIC_SUPPORT_EMAIL="support@example.com"
#
# Public support phone shown in the footer (optional)
# EXPO_PUBLIC_SUPPORT_PHONE="+256 700 000000"

# ============================================
# Notes
# ============================================
# - Changes to `.env` require a restart: `expo start --clear`
# - Set production env vars in CI/CD / EAS, not in a committed file

